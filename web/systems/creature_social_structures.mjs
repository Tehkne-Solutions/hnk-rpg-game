const clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,v));
export function socialBond({familiarity=.5,trust=.5,kinship=0,sharedSuccess=0,conflict=0}={}){return clamp(familiarity*.2+trust*.35+kinship*.2+sharedSuccess*.2-conflict*.25)}
export function roleFitness({traits={},skills={},needs={}}={}){return{SCOUT:clamp((traits.curiosity||0)*.4+(skills.navigation||0)*.4+(1-(needs.fatigue||0))*.2),GUARD:clamp((traits.boldness||0)*.35+(skills.defense||0)*.45+(1-(needs.fear||0))*.2),FORAGER:clamp((skills.foraging||0)*.5+(traits.persistence||0)*.3+(1-(needs.hunger||0))*.2),CAREGIVER:clamp((traits.nurture||0)*.5+(skills.care||0)*.35+(1-(needs.stress||0))*.15),LEADER:clamp((traits.confidence||0)*.25+(traits.sociality||0)*.2+(skills.communication||0)*.25+(skills.decision||0)*.3)}}
export function assignRole(member,availableRoles=[]){const fit=roleFitness(member);return [...availableRoles].map(role=>({role,score:fit[role]||0})).sort((a,b)=>b.score-a.score)[0]||null}
export function groupCohesion({members=[],conflicts=0,sharedTraditions=0,resourceStress=0}={}){if(!members.length)return 0;const bonds=members.flatMap((m,i)=>members.slice(i+1).map(n=>socialBond({familiarity:(m.familiarityTo?.[n.id]||0),trust:(m.trustTo?.[n.id]||0),kinship:(m.kinshipTo?.[n.id]||0)})));const avg=bonds.length?bonds.reduce((a,b)=>a+b,0)/bonds.length:.5;return clamp(avg*.55+sharedTraditions*.25-conflicts*.15-resourceStress*.2+.2)}
export function leadershipState({candidates=[],cohesion=.5,crisis=0}={}){const ranked=[...candidates].map(c=>({id:c.id,score:clamp((c.roleFitness?.LEADER||0)*.55+(c.prestige||0)*.2+(c.support||0)*.25+crisis*(c.crisisSkill||0)*.2)})).sort((a,b)=>b.score-a.score);return{leaderId:ranked[0]?.id||null,confidence:ranked[0]?clamp(ranked[0].score*(.6+.4*cohesion)):0,ranked}}
export function collectiveResponse({cohesion=.5,threat=0,youngDependents=0,resourceStress=0}={}){const defend=clamp(threat*.45+cohesion*.3+youngDependents*.2-resourceStress*.15);const flee=clamp(threat*.5+resourceStress*.25-cohesion*.2);const regroup=clamp(cohesion*.45+youngDependents*.25+threat*.15);return defend>=flee&&defend>=regroup?'DEFEND':flee>=regroup?'FLEE':'REGROUP'}
