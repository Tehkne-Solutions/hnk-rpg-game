const clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,v));
export function anomalyField({energyIntensity=0,convergencePower=0,instability=0,weatherCoupling=0,timeCoupling=0}={}){const severity=clamp(energyIntensity*.3+convergencePower*.3+instability*.2+weatherCoupling*.1+timeCoupling*.1);return{severity,active:severity>.25,volatile:severity>.65}}
export function anomalyRuleSet({gravity=1,timeRate=1,geometry=0,matterFlux=0,signalDistortion=0,spiritualPressure=0}={}){return{gravity:Math.max(.1,gravity),timeRate:Math.max(.05,timeRate),geometry:clamp(geometry),matterFlux:clamp(matterFlux),signalDistortion:clamp(signalDistortion),spiritualPressure:clamp(spiritualPressure)}}
export function applyZoneConditions(base={},rules={}){return{...base,gravityScale:rules.gravity??1,timeScale:rules.timeRate??1,geometryDistortion:rules.geometry||0,matterInstability:rules.matterFlux||0,signalDistortion:rules.signalDistortion||0,spiritualPressure:rules.spiritualPressure||0}}
export function anomalyWindow({hour=12,weather='CLEAR',energyPhase='QUIET',conditions=[]}={}){const matched=conditions.every(c=>c.type==='HOUR'?hour>=c.min&&hour<=c.max:c.type==='WEATHER'?c.value===weather:c.type==='PHASE'?c.value===energyPhase:true);return{open:matched,conditions:[...conditions]}}
export function recordObservation(log,{anomalyId,variable,value,context={},at=0,reliability=.5}={}){return[...(log||[]),{anomalyId,variable,value,context,at,reliability:clamp(reliability)}].slice(-64)}
export function inferRule(observations=[],{variable,minSamples=3}={}){const sample=observations.filter(o=>o.variable===variable);if(sample.length<minSamples)return{known:false,confidence:clamp(sample.length/minSamples)};const numeric=sample.map(o=>Number(o.value)).filter(Number.isFinite);if(!numeric.length)return{known:true,confidence:clamp(sample.reduce((n,o)=>n+o.reliability,0)/sample.length),pattern:'CATEGORICAL'};const avg=numeric.reduce((a,b)=>a+b,0)/numeric.length;const spread=Math.max(...numeric)-Math.min(...numeric);return{known:true,confidence:clamp(sample.reduce((n,o)=>n+o.reliability,0)/sample.length*(1-clamp(spread/(Math.abs(avg)||1))*.5)),average:avg,spread}}
export function experimentResult({prediction,observed,tolerance=.1}={}){const p=Number(prediction),o=Number(observed);if(!Number.isFinite(p)||!Number.isFinite(o))return{supports:false,error:null};const error=Math.abs(p-o);return{supports:error<=Math.max(tolerance,Math.abs(p)*tolerance),error}}
