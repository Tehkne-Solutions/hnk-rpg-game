const clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,v));
export function signal({channel='VOCAL',pattern='UNKNOWN',intensity=.5,context='NEUTRAL',sourceId=null,at=0}={}){return{channel,pattern,intensity:clamp(intensity),context,sourceId,at}}
export function interpretation({signal,lexicon={},contextFit=.5,observerSkill=.3,relationship=.5}={}){const known=lexicon?.[signal?.channel]?.[signal?.pattern]||null;const base=known?.confidence||0;const confidence=clamp(base*.5+contextFit*.2+observerSkill*.2+relationship*.1);return{meaning:known?.meaning||'AMBIGUOUS',confidence,ambiguous:confidence<.65,channel:signal?.channel||null,pattern:signal?.pattern||null}}
export function learnSignal(lexicon,{channel,pattern,meaning,success=false,confirmation=.5}={}){const next=structuredClone(lexicon||{});next[channel] ||= {};const prev=next[channel][pattern]||{meaning,confidence:0,observations:0};const evidence=clamp(confirmation)*(success?1:.45);next[channel][pattern]={meaning,confidence:clamp(prev.confidence*.82+evidence*.28),observations:(prev.observations||0)+1};return next}
export function misunderstandingRisk({interpretation,stress=0,signalNoise=0,culturalDistance=0}={}){return clamp((1-(interpretation?.confidence||0))*.5+clamp(stress)*.2+clamp(signalNoise)*.2+clamp(culturalDistance)*.1)}
export function composeResponse({intent='NEUTRAL',knownChannels=[],confidence=.5}={}){const channel=knownChannels[0]||'GESTURE';return{channel,intent,confidence:clamp(confidence),requiresFeedback:true}}
export function hnkSignalAffinity({observerSensitivity=0,exposure=0,resonanceMatch=0,interference=0}={}){return clamp(observerSensitivity*.35+exposure*.25+resonanceMatch*.4-interference*.3)}
export function crossSpeciesUnderstanding({lexiconCoverage=.2,relationship=.3,sharedContext=.4,signalQuality=.5,hnkAffinity=0}={}){return clamp(lexiconCoverage*.35+relationship*.2+sharedContext*.2+signalQuality*.15+hnkAffinity*.1)}
