const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
export function infectionChance(profile,{diseaseId,exposure=0,contact=.5,resistance=.5,care=[]}={}){const d=profile.diseases?.[diseaseId];if(!d)return 0;let scale=1;for(const id of care){const c=profile.care?.[id];if(c)scale*=c.transmissionScale}return clamp(d.transmission*exposure*(.5+.5*contact)*(1-.55*resistance)*scale,0,1)}
export function diseaseDay(profile,state,{diseaseId,care=[],days=1}={}){const d=profile.diseases?.[diseaseId];if(!d)return{...state};let recovery=d.recovery;for(const id of care){const c=profile.care?.[id];if(c)recovery+=c.recoveryBoost}const burden=clamp((state?.burden??.4)+d.severity*.06*days-recovery*.08*days,0,1);return{...state,burden,infected:burden>.03,health:clamp((state?.health??.8)-burden*d.severity*.06*days,0,1),day:(state?.day||0)+days}}
export function offspringTraits(profile,a={},b={},seed=.5){const out={};for(const[id,t]of Object.entries(profile.traits||{})){const av=a[id]??.5,bv=b[id]??.5,mid=(av+bv)/2,variation=(clamp(seed,0,1)-.5)*2*t.mutation;out[id]=clamp(mid+variation,t.min,t.max)}return out}
export function breedingScore(traits={},weights={DISEASE_RESISTANCE:.3,PRODUCTIVITY:.3,TEMPERAMENT:.2,FEED_EFFICIENCY:.2}){let s=0,w=0;for(const[k,v]of Object.entries(weights)){s+=(traits[k]??.5)*v;w+=v}return w?clamp(s/w,0,1):0}
export function selectBreeders(animals=[],weights){return[...animals].sort((a,b)=>breedingScore(b.traits,weights)-breedingScore(a.traits,weights))}
export function lineageRecord({offspringId,parentA,parentB,generation=1,traits}){return{offspringId,parentA,parentB,generation,traits}}
