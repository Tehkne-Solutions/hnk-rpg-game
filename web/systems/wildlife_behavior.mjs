const clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,v));
export function needState({hunger=0,thirst=0,fatigue=0,fear=0,territorial=0}={}){return{hunger:clamp(hunger),thirst:clamp(thirst),fatigue:clamp(fatigue),fear:clamp(fear),territorial:clamp(territorial)}}
export function encounterDisposition({temperament=.5,fear=.2,territorial=.2,familiarity=0,foodAttraction=0,threat=0}={}){const approach=clamp((1-temperament)*.2+familiarity*.45+foodAttraction*.35-threat*.5-fear*.35);const aggression=clamp(temperament*.35+territorial*.4+threat*.45-familiarity*.3);const flee=clamp(fear*.55+threat*.5-aggression*.25-familiarity*.25);return{approach,aggression,flee,behavior:aggression>.62?'DEFEND':flee>.58?'FLEE':approach>.55?'APPROACH':'OBSERVE'}}
export function rememberEncounter(memory,{actorId,outcome,kind='CONTACT',at=0,valence=0}={}){const events=[...(memory?.events||[]),{actorId,outcome,kind,at,valence:clamp(valence,-1,1)}].slice(-24);const familiarity=clamp((memory?.familiarity||0)+.08);const trust=clamp((memory?.trust||.5)+clamp(valence,-1,1)*.12);return{events,familiarity,trust}}
export function migrationDrive({food=.5,water=.5,habitat=.5,climateStress=0,disturbance=0,seasonalDrive=0}={}){const deficit=(1-clamp(food))*.3+(1-clamp(water))*.3+(1-clamp(habitat))*.2+clamp(climateStress)*.1+clamp(disturbance)*.1;return clamp(deficit+clamp(seasonalDrive)*.35)}
export function chooseMigrationTarget(targets=[]){return [...targets].map(t=>({...t,score:clamp((t.food||0)*.28+(t.water||0)*.28+(t.habitat||0)*.24+(t.safety||0)*.2-(t.travelCost||0)*.25)})).sort((a,b)=>b.score-a.score)[0]||null}
export function settlementConflictRisk({cropAttraction=0,wasteAttraction=0,habitatLoss=0,traffic=0,huntingPressure=0,barrierQuality=0}={}){return clamp(cropAttraction*.22+wasteAttraction*.2+habitatLoss*.25+traffic*.12+huntingPressure*.21-barrierQuality*.25)}
export function bondPotential({memory,careConsistency=.5,stress=0,speciesAffinity=.5}={}){return clamp((memory?.trust||.5)*.4+(memory?.familiarity||0)*.25+careConsistency*.2+speciesAffinity*.15-stress*.35)}
