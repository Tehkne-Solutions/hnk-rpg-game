const clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,v));
export function inheritTraits({parentA={},parentB={},mutationRate=.04,hnkExposure=0,stability=.8}={}){const keys=new Set([...Object.keys(parentA),...Object.keys(parentB)]);const traits={};for(const k of keys){const a=Number(parentA[k]??.5),b=Number(parentB[k]??.5);const inherited=(a+b)/2;const hnkShift=(clamp(hnkExposure)*.18)*(1-clamp(stability));const mutation=(Math.random()<mutationRate?(Math.random()-.5)*.2:0);traits[k]=clamp(inherited+hnkShift+mutation)}return traits}
export function phenotypeFromTraits({traits={},environment={},hnkAffinity=0}={}){const adaptation=Object.values(environment).reduce((s,v)=>s+clamp(Number(v)||0),0)/Math.max(1,Object.keys(environment).length);const expression={};for(const [k,v] of Object.entries(traits))expression[k]=clamp(v*.75+adaptation*.15+clamp(hnkAffinity)*.1);return expression}
export function transformationInheritance({parentTransformations=[],stability=.7,hnkAffinity=.5,exposure=0}={}){return parentTransformations.map(t=>({id:t.id,inheritanceChance:clamp((t.heritable??.5)*.5+clamp(stability)*.2+clamp(hnkAffinity)*.2+clamp(exposure)*.1),stability:clamp((t.stability??.5)*.65+clamp(stability)*.35)}))}
export function mutationPressure({pollution=0,radiation=0,hnkFlux=0,stress=0,biodiversity=1}={}){return clamp(pollution*.2+radiation*.25+hnkFlux*.35+stress*.15+(1-clamp(biodiversity))*.05)}
export function lineageDiversity(individuals=[]){if(!individuals.length)return 0;const traitKeys=new Set(individuals.flatMap(i=>Object.keys(i.traits||{})));if(!traitKeys.size)return 0;let total=0;for(const k of traitKeys){const vals=individuals.map(i=>Number(i.traits?.[k]??.5));const mean=vals.reduce((a,b)=>a+b,0)/vals.length;const variance=vals.reduce((s,v)=>s+(v-mean)**2,0)/vals.length;total+=Math.sqrt(variance)}return clamp(total/traitKeys.size*3)}
export function transformationStability({base=.7,hnkFlux=0,care=.5,nutrition=.5,stress=0}={}){return clamp(base*.45+(1-clamp(hnkFlux))*.2+care*.15+nutrition*.15-stress*.2+.05)}
export function offspringIdentity({lineageId,generation,parentIds=[],traits={},transformations=[]}={}){return{id:`CR:${lineageId}:${generation}:${parentIds.join('-')||'unknown'}`,lineageId,generation,parentIds:[...parentIds],traits:{...traits},transformations:transformations.map(t=>({...t}))}}
