const clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,v));
export function conflictPressure({scarcity=0,grievance=0,borderPressure=0,fear=0,communication=.5,sharedBenefit=0}={}){return clamp(scarcity*.28+grievance*.28+borderPressure*.22+fear*.12-communication*.12-sharedBenefit*.18)}
export function raidDecision({pressure=0,readiness=.5,expectedGain=.5,expectedLoss=.5,peaceSignal=0}={}){const score=clamp(pressure*.35+readiness*.25+expectedGain*.25-expectedLoss*.3-peaceSignal*.35);return{score,action:score>=.68?'RAID':score>=.48?'PROBE':score>=.3?'SHOW_FORCE':'HOLD'}}
export function defenseResponse({threat=.5,cohesion=.5,readiness=.5,youngAtRisk=0,escapeRoute=.5}={}){const defend=clamp(threat*.32+cohesion*.24+readiness*.24+youngAtRisk*.2);const retreat=clamp(threat*.35+(1-readiness)*.25+escapeRoute*.25+(1-cohesion)*.15);return{defend,retreat,action:defend>=.62&&defend>=retreat?'DEFEND':retreat>=.55?'RETREAT':'REGROUP'}}
export function resolveClash({attack=.5,defense=.5,cohesionA=.5,cohesionB=.5}={}){const a=clamp(attack*(.7+.3*cohesionA)),b=clamp(defense*(.7+.3*cohesionB)),delta=a-b;return{advantage:delta,outcome:Math.abs(delta)<.12?'STALEMATE':delta>0?'ATTACKER_ADVANTAGE':'DEFENDER_ADVANTAGE',lossA:clamp(b*.22),lossB:clamp(a*.22)}}
export function ceasefirePotential({trust=.3,exhaustion=.3,sharedThreat=0,communication=.5,grievance=.5}={}){return clamp(trust*.22+exhaustion*.28+sharedThreat*.22+communication*.22-grievance*.24)}
export function updateConflictMemory(memory={},event={}){const severity=clamp(event.severity||0);const cooperative=clamp(event.cooperation||0);return{grievance:clamp((memory.grievance||0)+severity*.18-cooperative*.12),trust:clamp((memory.trust||.4)+cooperative*.14-severity*.16),trauma:clamp((memory.trauma||0)+severity*.16),exhaustion:clamp((memory.exhaustion||0)+severity*.12),events:[...(memory.events||[]),event].slice(-32)}}
export function peaceRecovery(memory={}, {safeDays=0,cooperation=0,restitution=0,communication=.5}={}){const factor=clamp(safeDays/30)*.18+clamp(cooperation)*.18+clamp(restitution)*.22+clamp(communication)*.12;return{...memory,grievance:clamp((memory.grievance||0)-factor*.55),trust:clamp((memory.trust||0)+factor*.45),trauma:clamp((memory.trauma||0)-factor*.25),exhaustion:clamp((memory.exhaustion||0)-factor*.35)}}
